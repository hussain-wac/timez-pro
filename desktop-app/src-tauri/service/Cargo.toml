[package]
name = "timez-service"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
dbus = "0.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
timez-core = { path = "../core" }
notify-rust = "4"
